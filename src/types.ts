export enum Routes {
  Home = "/home",
  ClientSide = "/client-side",
  ServerSide = "/server-side",
  StaticRendering = "/static-rendering",
  Logout = "./",
}

export type Country = {
  code: string;
  name: string;
  emoji: string;
};
